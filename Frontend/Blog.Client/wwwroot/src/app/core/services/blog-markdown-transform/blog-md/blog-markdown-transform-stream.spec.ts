import { BlogMarkdownTransformStream } from './blog-markdown-transform-stream';

describe('BlogMarkdownTransformStream', () => {
  it('should create an instance', () => {
    expect(new BlogMarkdownTransformStream()).toBeTruthy();
  });
});
